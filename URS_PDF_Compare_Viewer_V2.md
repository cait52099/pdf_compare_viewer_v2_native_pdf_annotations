# URS — 多窗口 PDF 对照阅读器（路线 A / PySide6 + QPdfView + PyMuPDF）

## 1. 文档信息
- 文档名称：URS — 多窗口 PDF 对照阅读器
- 版本：V2.0 Draft
- 语言：中文
- 目标平台：Windows 优先，兼容 macOS
- 目标用户：需要对照阅读 2~3 份 PDF 文件的工艺/质量/技术文档用户

## 2. 项目目标
开发一套桌面工具，用于同时打开 2 份或 3 份 PDF 文件进行同步对照阅读。系统启动后按照文件数量自动生成对应数量的阅读窗口，并在主屏幕默认排布为左右或左中右布局。用户可继续手动拖动窗口到双屏或三屏环境。系统支持统一滚动、统一翻页、统一缩放、单窗口独立操作，以及恢复默认布局与恢复初始对比缩放。

## 3. 使用场景
1. 英文电子版与日文扫描件对照阅读。
2. 英文电子版、日文扫描件、比利时文扫描件三方对照阅读。
3. 用户先进行同页同步阅读，再对某一窗口局部放大或单独滚动检查细节。
4. 用户使用双屏/三屏办公环境，将不同文件拖到不同显示器中阅读。

## 4. 范围
### 4.1 本期范围（In Scope）
- 选择 2 或 3 份 PDF 文件。
- 根据文件数量创建 2 或 3 个阅读窗口。
- 主屏默认布局：
  - 2 文件：左右平铺。
  - 3 文件：左中右平铺。
- 用户可手动拖动窗口到其他显示器。
- 统一上下滚动、统一左右滚动。
- 键盘方向键统一滚动。
- Page Up / Page Down 统一翻页。
- 统一放大、统一缩小。
- 统一恢复初始对比缩放。
- 单窗口独立滚动、独立缩放、独立旋转。
- 恢复默认布局。
- PDF 批注能力预留接口。

### 4.2 非本期范围（Out of Scope）
- OCR 自动识别步骤号并自动对齐。
- 自动多语言内容匹配。
- 非 PDF 文档的完整原生编辑。
- 云同步、多用户协作。
- 自动根据多屏拓扑智能排屏。

## 5. 功能需求
### FR-01 文件导入
系统应允许用户在启动时依次选择 2 或 3 份 PDF 文件。
- 电子版 PDF：必选。
- 扫描件1 PDF：必选。
- 扫描件2 PDF：可选。
- 若第二份未选择，则流程终止。
- 若第三份未选择，则进入双窗口模式。

### FR-02 窗口数量
系统应根据选择的 PDF 文件数量创建等数量的阅读窗口。
- 2 文件 -> 2 个阅读窗口。
- 3 文件 -> 3 个阅读窗口。

### FR-03 默认布局
系统应在主屏可用区域内按文件数量自动排布窗口。
- 2 文件：左右平铺。
- 3 文件：左中右平铺。
- 默认布局仅影响窗口位置和大小，不改变用户后续手动拖动自由。

### FR-04 恢复默认布局
系统应提供“恢复默认布局”功能。
- 按当前文件数量恢复到主屏默认平铺布局。
- 不改变当前页码。
- 不改变当前缩放比例。
- 不改变当前旋转状态。

### FR-05 同步模式与单窗口模式
系统应支持两种操作模式：
- 同步模式：操作作用于全部阅读窗口。
- 单窗口模式：操作仅作用于当前活动窗口。

### FR-06 鼠标滚动控制
在同步模式下：
- 鼠标滚轮统一上下滚动全部窗口。
- 横向滚动统一左右滚动全部窗口。

在单窗口模式下：
- 鼠标滚轮仅滚动当前活动窗口。

### FR-07 键盘方向键控制
在同步模式下：
- ↑：全部窗口统一向上滚动。
- ↓：全部窗口统一向下滚动。
- ←：全部窗口统一向左滚动。
- →：全部窗口统一向右滚动。

在单窗口模式下：
- 方向键仅作用于当前活动窗口。

### FR-08 键盘翻页控制
在同步模式下：
- Page Up：全部窗口统一翻到上一页，并定位至该页顶部。
- Page Down：全部窗口统一翻到下一页，并定位至该页顶部。

在单窗口模式下：
- Page Up / Page Down 仅作用于当前活动窗口。

### FR-09 统一缩放
系统应支持统一缩放操作：
- 统一放大。
- 统一缩小。
- 统一恢复初始对比缩放。

“初始对比缩放”定义为：每个文件完成初次默认布局后记录的默认阅读缩放比例，用于快速回到适合同页对比的状态。

### FR-10 单窗口缩放
系统应允许用户对单个阅读窗口单独进行放大、缩小与恢复初始缩放。

### FR-11 单窗口旋转
系统应允许用户对单个阅读窗口单独进行：
- 向左旋转 90°
- 向右旋转 90°
- 恢复 0°

### FR-12 窗口大小变化行为
用户调整阅读窗口大小时，系统不应自动改变当前 PDF 的缩放比例。

### FR-13 焦点与模式切换
- 点击某一阅读窗口后，该窗口成为活动窗口。
- 用户可通过按钮或 Esc 返回同步模式。

### FR-14 页码状态显示
每个阅读窗口应显示当前页码 / 总页数。

### FR-15 批注扩展接口
系统应为后续 PDF 批注与修改能力预留接口，包括：
- 高亮
- 矩形框选
- 文字注释
- 保存为新 PDF

## 6. 非功能需求
### NFR-01 性能
- 打开常规工艺 PDF 时，窗口初始化应尽量在可接受时间内完成。
- 同步滚动应保持平滑，不出现明显卡死。

### NFR-02 易用性
- 主控操作应清晰区分“全局控制”和“单窗口控制”。
- 恢复默认布局与恢复初始缩放应可一键触达。

### NFR-03 可维护性
- 采用模块化架构，分离 UI、同步控制、文档适配、批注写回逻辑。

### NFR-04 兼容性
- Windows 为首发平台。
- 兼容多屏环境。
- 兼容高 DPI 显示环境。

## 7. 业务规则
- 只有选择 2 份及以上文件后才能进入阅读器。
- 所有统一控制只对当前打开的阅读窗口集合生效。
- 恢复默认布局仅重置窗口位置和尺寸，不重置文档状态。
- 恢复初始缩放仅重置缩放，不重置页码、滚动位置或旋转。

## 8. 验收标准
### AC-01 双文件默认布局
给定 2 份 PDF，启动后系统应自动创建 2 个阅读窗口，并在主屏幕左右平铺显示。

### AC-02 三文件默认布局
给定 3 份 PDF，启动后系统应自动创建 3 个阅读窗口，并在主屏幕左中右平铺显示。

### AC-03 用户拖动多屏
用户将窗口拖到第二或第三显示器后，系统应继续保持同步模式可用。

### AC-04 统一滚动
在同步模式下，鼠标滚轮或方向键操作应同时作用于全部窗口。

### AC-05 单窗口独立控制
点击某一窗口进入单窗口模式后，滚动、缩放和翻页应只影响该窗口。

### AC-06 恢复默认布局
用户打乱窗口布局后，点击“恢复默认布局”应恢复主屏默认平铺，不改变文档状态。

### AC-07 恢复初始缩放
用户对一个或多个窗口进行局部放大后，点击“恢复初始缩放”应恢复至该文件初始对比比例。

### AC-08 统一翻页
在同步模式下，Page Up / Page Down 应使所有窗口同步翻页。

### AC-09 窗口大小调整
用户改变窗口大小后，当前 PDF 缩放比例应保持不变。

## 9. 后续版本候选
- OCR 步骤号锚点识别与对齐。
- 搜索与多窗口高亮同步。
- 批注工具栏与 PDF 写回。
- 非 PDF 文档适配层（图片、XPS、EPUB 等）。
